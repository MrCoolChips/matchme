<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Matchme"
        tools:targetApi="31">

        <!-- Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity
            android:name=".presentation.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.auth.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".AccueilActivity"
            android:exported="false" />

        <!-- Définition du Profil -->
        <activity
            android:name=".DefProfilActivity"
            android:screenOrientation="landscape"
            android:exported="false" />

        <!-- Profil -->
        <activity
            android:name=".presentation.profil.EditProfilActivity"
            android:exported="false" />
    </application>

</manifest>